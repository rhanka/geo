apiVersion: v1
kind: Service
metadata:
  name: geo-api
  labels:
    app.kubernetes.io/name: geo-api
    app.kubernetes.io/part-of: geo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: geo-api
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
