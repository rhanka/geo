# postgis — la base servie par geo-api.
#
# Elle tournait en production SANS EXISTER DANS LE DÉPÔT : seul son Service
# (`geo-postgis-service.yaml`) était committé. Personne n'aurait pu la rebâtir
# depuis un checkout propre, et sa limite mémoire — qui plafonnait la capture —
# n'était modifiable que par un `kubectl edit` à la main.
#
# Exporté depuis le cluster OVH, à l'identique, sauf la limite mémoire.
#
# LIMITE MÉMOIRE : 1536 Mi -> 768 Mi
#
# `ResourceQuota` compte les LIMITES, pas les demandes. postgis demandait
# 160 Mi et réservait 1536 Mi — 9,6× — soit 37,5 % du quota du namespace, ce
# qui interdisait le 3ᵉ pod de capture. 768 Mi reste 4,8× la demande.
#
# ⚠️ Cette valeur n'est PAS mesurée : `kubectl top` est refusé par le RBAC
# scopé, et le pod affiche 0 redémarrage et aucun `lastState.terminated` —
# ce qui prouve qu'il n'a jamais été OOM-killed, pas qu'il reste sous 768 Mi.
# Le jour où un `OOMKilled` apparaît ici, la cause est ce commit.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgis
  namespace: geo
  labels:
    app.kubernetes.io/name: postgis
    app.kubernetes.io/part-of: geo
spec:
  replicas: 1
  serviceName: geo-postgis
  podManagementPolicy: OrderedReady
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  selector:
    matchLabels:
      app.kubernetes.io/name: postgis
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      partition: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgis
        app.kubernetes.io/part-of: geo
    spec:
      containers:
        - name: postgis
          image: postgis/postgis:16-3.4
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgresql
              containerPort: 5432
              protocol: TCP
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: geo-postgis-credentials
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: geo-postgis-credentials
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: geo-postgis-credentials
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", 'pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"']
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 6
          readinessProbe:
            exec:
              command: ["/bin/sh", "-c", 'pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"']
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 6
          resources:
            requests:
              # 30m -> 15m (2026-09-25, nœud OVH unique saturé en requests CPU).
              # Mesuré cgroup v2 sur postgis-0 (11,75 j) : moyenne 9,2m sur la vie
              # du conteneur, 9,9m sur une fenêtre de 89 s — consommation stable.
              # 15m = moyenne × 1,5. Le live tourne à 10m depuis un
              # `kubectl set resources` hors dépôt du 2026-09-14 : c'est sans marge
              # au-dessus de la moyenne, ne pas descendre plus bas.
              #
              # ⚠️ Aucune limite CPU déclarée ici : le LimitRange du namespace
              # (poc-k8s, tenants/geo) injecte `default.cpu: 100m`. Mesuré :
              # 69,8 % des périodes CFS actives sont throttlées à ce plafond
              # (cpu.stat nr_throttled/nr_periods). Sujet distinct des requests,
              # non modifié ici (quota limits.cpu, redémarrage du pod).
              cpu: 15m
              memory: 160Mi
            limits:
              memory: 768Mi
          volumeMounts:
            - name: pg-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: pg-data
        labels:
          app.kubernetes.io/name: postgis
          app.kubernetes.io/part-of: geo
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: block-standard
        volumeMode: Filesystem
        resources:
          requests:
            storage: 5Gi
