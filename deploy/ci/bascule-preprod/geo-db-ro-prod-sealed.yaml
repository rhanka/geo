# PLACEHOLDER COMMENTÉ — SealedSecret geo-db-ro-prod (ns geo).
# Structure identique à radar-immobilier:deploy/ci/bascule-preprod/radar-db-ro-prod-sealed.yaml.
# Valeurs en clair (jamais committées) : POSTGRES_USER=geo_db_ro_prod, POSTGRES_PASSWORD=<openssl rand>,
#   POSTGRES_DB=<nom de la DB prod geo = geo-postgis-credentials/POSTGRES_DB>.
# SCELLÉ ET COMMITTÉ PAR geo-cond (kubeseal strict, lié ns+name) à la place de ce fichier. Tant qu'il n'y a
# pas de document `kind: SealedSecret` actif, bascule-bundle-cd.yml et install-cd-bootstrap.sh REFUSENT
# d'appliquer le bundle (fail-closed).
#
# ---
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: geo-db-ro-prod
#   namespace: geo
# spec:
#   encryptedData:
#     POSTGRES_DB: <kubeseal>
#     POSTGRES_PASSWORD: <kubeseal>
#     POSTGRES_USER: <kubeseal>
#   template:
#     metadata:
#       name: geo-db-ro-prod
#       namespace: geo
#     type: Opaque
