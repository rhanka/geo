# immo-lots clean — 2026-07-13T004133Z

## Résultat

- `qc-lots` déposés avant : **825**
- `qc-lots` déposés après : **825**
- gap avant : **1**
- gap après : **1**
- backfills réussis : **0**
- artefacts purgés : **0**

## Classification

- Vrai slug restant : `saint-marc-du-lac-long` — municipalité confirmée dans `packages/qc-sources/src/geo/municipalities.qc.json` et `work/coverage/coverage-matrix.json`.
- Productibilité : non prouvée. Le slug est absent de `normalized/qc-cadastre-lots/` et de `raw/`; `lot-zone-join-run.ts` et `lots-enriched-run.ts --no-role` ont tous deux retourné `skipped=1` pour cadastre absent.
- Artefacts dans le gap : aucun.
- Artefacts zonage inspectés : aucun match pour `geojson.`, `__subdir`, `__qc-zonage` ou `-arcgis` sous `normalized/ca-qc-zonage/`.
- Purge : aucune exécution destructive; aucun candidat sûr à passer à `zones-purge.ts`.

## Comptage final

`qc-lots-gap.ts --json` : `served=919`, `zones=787`, `norms=715`, `qc_lots_deposited=825`, `up_to_date=799`, `gap=1`.

Le gap final contient uniquement `saint-marc-du-lac-long`, vraie muni mais non productible avec les dépôts disponibles. Les `119` entrées `norms_only` sont hors gap et non productibles par conception car elles n'ont pas de couche zones.

## Reconcile

`coverage-reconcile.ts` exécuté officiellement; scoreboard : `pv=1059`, `normes=716`, `zones=826`, `cadastre=1106`, `role-foncier=1106`, `tod=39`.
