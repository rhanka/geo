# FOCUS30 zones - 2026-07-07

## Portee

Objectif: terminer le focus-30 zones avec `lile-dorval` et corriger les reacquisitions `dollard-des-ormeaux` / `saint-bruno-de-montarville` si les gates stricts le permettent.

Premiere lecture effectuee: `acquisition/src/loop-supervise.ts`, scripts `focus30-*`, audit reacquisition, publication zonage, checks S3, extraction T1/T2, GCP et verification overlap.

## Etat des gates

- `focus30-status`: 29/30 zonages servis; manquant S3 confirme: `lile-dorval`.
- `focus30-allayers`: matrice locale a 30 zones, 29 normes, 29 PV; `lile-dorval` manque normes/PV et n'a pas d'objet S3.
- `coverage-reconcile`: `/1106` = pv 1043, normes 554, zones 761, cadastre 1106, role-foncier 1106, tod 39.
- `zones-s3-check lile-dorval dollard-des-ormeaux saint-bruno-de-montarville`: `lile-dorval` absent; les deux autres presents.
- `zonage-reacquire-audit`: focus30 = 30 zones, ok 22, reacquire 2, normes suspect 1, indeterminate 5.

## Lile-Dorval

Resultat: non publie.

Constats:

- Le site public et le sitemap ne donnent pas de page reglements/zonage exploitable.
- Les documents de type portail ne sont pas utilises ici, car le site les exclut explicitement de l'exploration automatisee.
- Les rapports internes precedents classent deja `lile-dorval` en cas sans plan public recuperable / ne pas remplir par invention.
- La matrice `coverage-matrix.json` marque `zones.status=done`, mais S3 confirme l'absence de `normalized/ca-qc-zonage` pour ce slug.

Decision gate: aucun objet zones n'est depose sans source officielle verifiable.

## Dollard-des-Ormeaux

Resultat: non republie.

Constats:

- Objet zones S3 present: 69 entites avec 69 codes distincts, style local ancien (`C1a`, `P25`, `Pb1`, etc.).
- Produit normes present: 84 lignes, 32 codes distincts, style reglement 2025 (`C-200`, `C-202`, etc.).
- Overlap codes SIG/normes = 0, avant et apres canonisation.
- Essai non destructif sur le PDF officiel `R-2025-199`, page 144:
  - extraction texte: 1 code distinct, sous le seuil;
  - extraction OCR: 24 labels, 19 entites distinctes et 97.6% des lots affectes, mais codes bruites (`TR3n`, `R510-77`, `T5852`, etc.).

Decision gate: l'OCR brut n'est pas assez fiable pour ecraser le zonage publie. Il faut une passe vision/dictionnaire ou validation humaine des labels 2025 avant depot.

## Saint-Bruno-de-Montarville

Resultat: non republie.

Constats:

- Objet zones S3 present: 309 entites avec 309 codes distincts.
- Produit normes present: 20 codes distincts.
- Overlap codes SIG/normes = 0.
- GCP automatique disponible et georeferencement deja bon, mais l'extraction texte des labels de plan donne trop peu de codes propres pour publier une recomposition.

Decision gate: reacquisition a reprendre par extraction vision/points de labels, puis verification overlap normes avant depot.

## Conclusion

Aucun depot S3 n'a ete effectue pendant ce lot. Le focus-30 reste a 29/30 cote S3, avec `lile-dorval` comme manque confirme mais non sourcable proprement dans les limites strictes. Les deux reacquisitions restent ouvertes: elles demandent une etape d'extraction/validation des codes, pas une simple recomposition automatique.
