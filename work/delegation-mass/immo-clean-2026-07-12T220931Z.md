# immo-lots — nettoyage et backfill

- Date: 2026-07-12T22:09:31Z
- Branche: `feat/cadre-acquisition`
- Lane: claude
- Méthode: audits S3 et outils Node/TS committés; aucune modification manuelle de `coverage-matrix.json`.

## État avant

Audit `qc-lots-gap --json` après `loop-supervise`:

- served: 919 (`zones=786`, `norms=697`)
- qc-lots déposés: 824
- qc-lot-zonage déposés: 703
- up-to-date: 797
- gap réel: 2 — `mont-carmel`, `saint-marc-du-lac-long`
- norms-only non productibles: 120

Les deux slugs du gap sont des municipalités réelles présentes dans `municipalities.qc.json`. La matrice leur indique un cadastre `done`; la disponibilité S3 a été vérifiée séparément.

## Backfill

- `mont-carmel`: jointure zonage réussie, 49 lots, parquet et stats vérifiés; enrichissement réussi avec surface 100 %, code postal 100 % (RTA), zone_code 100 %, 49 lots déposés.
- `saint-marc-du-lac-long`: jointure et enrichissement sautés proprement; aucune clé exacte `normalized/qc-cadastre-lots/saint-marc-du-lac-long.geojson` et aucune variante S3 contenant `saint-marc` ne correspond à cette municipalité. Le résolveur de `lot-zone-join-run.ts` ne trouve donc aucun cadastre exploitable.

## Purge des artefacts

Lectures S3 sous `normalized/ca-qc-zonage/`:

- motif `.geojson.`: 0 clé
- motif `__subdir`: 0 clé
- motif `__qc-zonage`: 0 clé
- suffixe `-arcgis`: 0 clé

Aucun dépôt ne satisfaisait le double garde-fou. Aucune purge n’a été exécutée; nombre d’artefacts purgés: **0**. Aucune vraie municipalité n’a été supprimée.

## Réconciliation et état après

`coverage-reconcile.ts` a confirmé le scoreboard officiel `/1106`: `pv=1059`, `normes=698`, `zones=825`, `cadastre=1106`, `role-foncier=1106`, `tod=39`.

Audit final `qc-lots-gap`:

- served: 919 (`zones=786`, `norms=697`)
- qc-lots déposés: 824
- qc-lot-zonage déposés: 703
- up-to-date: 798
- gap: **1**
- gap restant: `saint-marc-du-lac-long` (`missing`, source zones 2026-07-03 21:07, cadastre exact absent)
- vrais producibles restants: **0**; le reste est prouvé non productible avec les dépôts disponibles
- norms-only non productibles: 120

Variation: `up_to_date` 797 → 798 et gap 2 → 1. Le cardinal `qc-lots déposés` reste 824 car le dépôt de `mont-carmel` a été remplacé/revalidé sous la même clé.

## Commit

Aucun correctif source n’était nécessaire. Ce rapport est le seul livrable committable de cette boucle.
