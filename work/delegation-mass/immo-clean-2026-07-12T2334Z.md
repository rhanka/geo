# Rapport immo-lots clean — 2026-07-12T2334Z

## Périmètre

- Branche: `feat/cadre-acquisition`.
- Node/TS uniquement.
- `.claude/`, `.track/` et les secrets n’ont pas été touchés.
- `coverage-matrix.json` n’a pas été édité à la main; la réconciliation a été exécutée par `coverage-reconcile.ts`.

## Mesures observées

La première exécution de `loop-supervise.ts` a donné `821` qc-lots servis au scoreboard. Le premier audit S3 `qc-lots-gap.ts --json` de cette session donnait déjà:

- qc-lots déposés: `825`;
- qc-lot-zonage déposés: `704`;
- municipalités servies: `919` (`787` zones, `709` normes);
- qc-lots à jour: `799`;
- gap: `1`.

Après la boucle, la mesure finale est inchangée: `825` qc-lots déposés, `799` à jour, gap `1`.

## Classement du gap

- Vrai nom de municipalité: `saint-marc-du-lac-long` est présent dans `packages/qc-sources/src/geo/municipalities.qc.json` et n’est pas exclu.
- Productibilité effective: `cad=N`; aucune clé S3 exacte `normalized/qc-cadastre-lots/saint-marc-du-lac-long.geojson` ni pré-clip correspondant n’existe.
- Tentative backfill: `lot-zone-join-run.ts --slugs saint-marc-du-lac-long` a été exécuté; il a skippé proprement faute de cadastre (`ok=0 skipped=1`). `lots-enriched-run.ts` n’a pas été lancé, puisqu’aucun parquet zonage ne pouvait être produit.
- Conclusion: `0` vrai producible restant dans le gap selon le critère demandé « avec cadastre »; le résidu est une vraie muni mais non productible dans l’état S3 courant.

## Artefacts zonage

Lecture S3 de `normalized/ca-qc-zonage/`: `1041` clés inspectées. Aucun motif artefact demandé n’est présent:

- `.geojson.`: `0`;
- `__subdir`: `0`;
- `__qc-zonage`: `0`;
- timestamp `YYYY-MM-DDT`: `0`;
- suffixe `-arcgis`: `0`.

Le dry-run explicite de `zones-purge.ts` sur quatre noms artefacts connus (`mont-tremblant.geojson.2026-07-07T1404Z`, `mont-tremblant.geojson.2026-07-07T1350Z`, `saint-nazaire-de-dorchester__subdir.2026-07-06T1606Z`, `sherbrooke-arcgis`) a donné `DRY-RUN slugs=4 keys=0`.

- Artefacts purgés: `0`.
- Aucune vraie municipalité n’a été supprimée.

## Reconciliation et contrôle final

`coverage-reconcile.ts` a été exécuté officiellement. Son scoreboard affiche `cadastre=1106` dans la matrice, mais la lecture directe S3 du préfixe cadastre ne trouve pas le slug final; la matrice est donc contradictoire avec l’entrée nécessaire au producteur. Aucun dépôt de cadastre n’a été inventé.

Le contrôle texte final confirme:

`GAP=1`, `missing=1`, `stale=0`, `gap_without_exact_cadastre=1`, `NEED saint-marc-du-lac-long ... cad=N`.

## Résultat

- qc-lots backfillés effectivement dans cette session: `0`;
- qc-lots tentés et skippés sans écriture: `1` (`saint-marc-du-lac-long`);
- artefacts zonage purgés: `0`;
- vrai gap final: `saint-marc-du-lac-long`, non productible prouvé faute de cadastre S3 exact;
- seul livrable committable de cette session: ce rapport; aucun correctif d’outil n’a été créé.
