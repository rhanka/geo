# Bilan 4A — collections B' sans effet densifiant connu

Le bilan est en lecture seule. Les sources nommées ferment `absent` à `59 + 45 - 8 = 96`, puis `unknown_only` ajoute 38 collections. Le résultat nominatif est donc 134, alors que le cahier des charges annonce 133 (`141 - 8`). Le snapshot 4A fourni ne compte que 7 `known_effect`; aucune pièce nommée ne permet d'identifier le huitième. Aucune collection n'a été retirée pour forcer le total.

Le JSON contient les 134 slugs, la cause la plus proche de l'action, la mesure ou citation verbatim, le statut et la reprise précise. Les recherches infructueuses restent `REPRENABLES`; les statuts `TERMINALE` reposent sur un constat explicite (projet/avis, code absent, ou absence verbatim dans le document audité).

Partition: A 17 · B 30 · C 47 · D 6 · E 10 · F 0 · G 14 · H 10. Total: 134.

Le premier effort utile est C (47): rattacher chaque valeur à une date légale verbatim. Le second est B (30): fournir une colonne/valeur de densité numérique dans une source en vigueur. La fermeture exacte à 133 reste bloquée par le delta de mesure `known_effect` (7 observés contre 8 demandés).
