# Dataset GeoJSON files are served from here as /data/<id>.geojson.
# Files are produced by the acquisition pipeline (@sentropic/geo-acquire) and
# are intentionally absent from source control. The UI degrades gracefully
# (empty-state) when a dataset's GeoJSON is missing.
