node_modules/
package-lock.json

# Sorties générées (régénérables via `npm run report`) — tracées sur disque,
# pas dans git. On garde le dossier out/ (voir .gitkeep) mais pas les artefacts.
out/*.md
out/*.docx
out/cron.log
!out/.gitkeep
