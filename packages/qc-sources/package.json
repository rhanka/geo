{
  "name": "@sentropic/geo-qc-sources",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Québec source adapters and bylaw/zonage parsers for the geo bylaw-orchestrator. Migrated from radar-immobilier @radar/sources (see MIGRATION.md).",
  "license": "UNLICENSED",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./municipalities": "./src/municipalities.ts",
    "./geo/geo-fetch-utils.js": "./src/geo/geo-fetch-utils.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^3.2.6"
  }
}
