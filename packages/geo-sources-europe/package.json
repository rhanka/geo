{
  "name": "@sentropic/geo-sources-europe",
  "version": "0.1.2",
  "type": "module",
  "description": "Manifestes (data) et recettes des sources géographiques d'Europe — France (IGN ADMIN EXPRESS, La Poste codes postaux, INSEE COG). Registre { manifests, recipes } consommé par @sentropic/geo (ADR-0017).",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rhanka/geo.git",
    "directory": "packages/geo-sources-europe"
  },
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src --passWithNoTests"
  },
  "dependencies": {
    "@sentropic/geo-core": "^0.1.0"
  }
}
